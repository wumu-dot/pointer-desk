# FEAT-20260620-001-SHT30-温湿度传感器集成

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。

## 0. 元数据
- **优先级**：P1(紧急)
- **预估总耗时**：2 小时
- **当前状态**：🟢已完成

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）
> SHT30 已到货 — 一步到位将 temp_mode 从 2 页（天气/设备信息）重构为 4 页架构（天气 → 湿度计 → 双温对比 → 设备信息），湿度计使用 SHT30 本地湿度数据，同时补上指针引擎湿度映射。

### 1.2 地基触碰前置自检
> 以下执行路径是否涉及 rules.md 第 8.1 条定义的"地基"？
- [ ] 是 → 立即进入地基评估流程，本 FEAT 暂停
- [x] 否 → 继续进入下方执行路线图

（子页枚举替换、新增 render 函数、指针引擎加一个函数均不触发地基条件）

### 1.3 任务边界
| 类型 | 具体内容 |
|------|----------|
| 范围内（可改） | `App/modes/temp_mode.c` — bool→enum 重构，新增 2 个 render 函数；`App/modes/temp_mode.h` — 如有需要加声明；`Middleware/pointer/pointer_engine.c` — 新增 `pointer_set_humidity()`；`Middleware/pointer/pointer_engine.h` — 声明 |
| 超出范围（禁止碰） | 不碰 `Drivers/BSP/sensor/temp_sensor.c`（SHT30 驱动已完整）；不碰 `Core/Src/stm32f4xx_hal_msp.c`（I2C2 PF0/PF1 已配置）；不碰其他 mode 文件（clock/timer/settings）；不新增 .c/.h 文件 |
| 外部依赖 | SHT30 模块 → PF0(SDA)/PF1(SCL)，3.3V/GND 从面包板取；ESP32 天气数据（`g_weather`）已有 |

## 2. 执行路线图

### 🟢 阶段 1：硬件接线 + 驱动验证（角色：Dev Agent）
- **阶段目标**：传感器上电 → I2C 探测成功 → 数据读取正确
- [ ] 步骤1：SHT30 模块接线 — VCC→面包板3.3V，GND→面包板GND，SCL→PF1，SDA→PF0
- [ ] 步骤2：上电，串口观察 `[SHT30] Device detected at 0x88 (I2C2)` 和 `[TEMP] Selected SHT30 (external, +-0.3C)` 日志
- [ ] 步骤3：切换到 temp_mode 设备信息页，确认 "STM32: xx.xC" 读数从内部ADC变为 SHT30 温度
- [ ] 步骤4：对着传感器哈气，观察温湿度数值变化，确认 `sht30_read()` 返回有效数据

### 🔵 阶段 2：指针引擎 — 湿度刻度映射（角色：Dev Agent）
- **阶段目标**：新增 `pointer_set_humidity()`，让物理指针能指向湿度百分比
- [ ] 步骤1：在 `pointer_engine.h` 的 `pointer_set_page()` 声明后添加 `void pointer_set_humidity(uint8_t percent);`
- [ ] 步骤2：在 `pointer_engine.c` 的 `pointer_set_page()` 函数后实现：`angle = 30.0f + (float)percent / 100.0f * 300.0f;`（30→330弧线），clamp 到 [0,100]，调用 `pointer_set_target(angle, POINTER_MOVE_NORMAL)`

### 🟠 阶段 3：temp_mode 4 页架构重构（角色：Dev Agent）
- **阶段目标**：将 `show_device_info` bool 替换为 `temp_page_t` 枚举，新增湿度计和双温对比两个子页

#### 3.1 架构替换
- [ ] 步骤1：删除 `static bool show_device_info` 和 `static bool needs_render_device_info`
- [ ] 步骤2：新增 `temp_page_t` 枚举：`TEMP_PAGE_WEATHER=0, TEMP_PAGE_HUMIDITY, TEMP_PAGE_COMPARISON, TEMP_PAGE_DEVICE, TEMP_PAGE_COUNT`
- [ ] 步骤3：新增 `static temp_page_t current_page = TEMP_PAGE_WEATHER;`
- [ ] 步骤4：更新 `init/enter` — `current_page` 重置为 WEATHER，删除 `show_device_info` 引用
- [ ] 步骤5：更新 `render` 分派 — `switch(current_page)` 调用 4 个 render 函数之一
- [ ] 步骤6：更新 `update` 分派 — 天气页→ESP32温度指针；湿度页→`temp_sensor_read()` 拿SHT30本地湿度→`pointer_set_humidity(local_humidity)`（SHT30离线时湿度=0,指针归位30度）；对比页→SHT30本地温度指针（离线回退ESP32温度）；设备页→不动
- [ ] 步骤7：更新按键处理 — 长按 `current_page = (current_page+1) % TEMP_PAGE_COUNT`，清去重缓存→清屏→标脏

#### 3.2 湿度计子页 `render_humidity_page()` — SHT30 本地湿度
- [ ] 步骤8：调用 `temp_sensor_read()` 获取 SHT30 本地湿度 `local_humidity`（float → uint8_t 取整）
- [ ] 步骤9：大字显示湿度百分比 `"XX%"`（font_id=3, 16x32），居中于 cy=40
- [ ] 步骤10：湿度等级标签 — <30 "Dry"(黄), <60 "Comfort"(绿), <80 "Humid"(青), >=80 "Wet"(蓝)，居中于 cy=85
- [ ] 步骤11：湿度环 `gui_draw_arc(64, 125, 30, 0, h*360/100, 4, color)`
- [ ] 步骤12：SHT30 离线降级 — 若 `temp_sensor_get_source() != TEMP_SRC_SHT30`，显示 "--%" + 标签 "No SHT30"（灰色），湿度环不画
- [ ] 步骤13：底部提示 "HOLD: next"（灰色小字），去重用 `static uint8_t last_humid` + `static temp_source_t last_src`

#### 3.3 双温对比子页 `render_comparison_page()`
- [ ] 步骤14：读取 `temp_sensor_read()` 获取本地 SHT30 数据，读取 `g_weather` 获取区域 ESP32 数据
- [ ] 步骤15：左右两栏布局 — 左侧 LOCAL(SHT30) 列标题青色，右侧 REGIONAL(ESP32) 列标题黄色，中间竖线分隔
- [ ] 步骤16：温度大字显示（font_id=2, 12x24），单位标签 C/F；湿度行 "H:xx%"
- [ ] 步骤17：底部温差行 `"Diff: +x.xC"` — diff>1C 红色，diff<-1C 蓝色，否则绿色
- [ ] 步骤18：SHT30 离线降级 — LOCAL 温度 "--.-"，来源 "None"（灰色），湿度 "--%"，温差行隐藏
- [ ] 步骤19：ESP32 无数据降级 — REGIONAL 显示 "--.-"，湿度 "--%"
- [ ] 步骤20：底部提示 "HOLD: next"（灰色小字），去重基于 local temp+hum + weather temp+hum 四元组

#### 3.4 ARMCC V5 兼容性
- [ ] 步骤21：检查无复合语句表达式（如 `({...})`）、无 `0b` 字面量、无双斜杠注释混用

===== 切换至 Test Agent =====

### 🟣 阶段 4：独立测试（角色：Test Agent）
- **阶段目标**：覆盖全部 4 页 + SHT30 在线/离线 + ESP32 有/无数据
- [ ] TC01 — SHT30 在线 + ESP32 有效：天气页显示 ESP32 温湿度；湿度计页显示 SHT30 本地湿度百分比；双温对比页 LOCAL(SHT30) vs REGIONAL(ESP32)，温差标签颜色合理；设备信息页 "STM32" 显示 SHT30 温度
- [ ] TC02 — SHT30 离线（拔 VCC 重启）：湿度计页显示 "--%" + "No SHT30"；双温对比页 LOCAL "--.-" + "None"，温差行隐藏；设备信息页回退内部 ADC
- [ ] TC03 — ESP32 无数据（`g_weather.valid=false`）：天气页显示 "Waiting ESP32..."；双温对比页 REGIONAL "--.-"
- [ ] TC04 — 长按 4 页轮转：天气→湿度计→双温对比→设备信息→天气，每页全屏重绘无残影，短按切模式正常
- [ ] TC05 — 回归：Clock 模式正常、Timer 模式正常、Settings 保存/退出正常
- [ ] TC06 — 编译：Keil ARMCC V5 Rebuild → 0 Error，`check-firmware.sh` 18 项通过
- [ ] 若失败退回 Dev Agent

===== 切换至 Review Agent =====

### 🔴 阶段 5：独立审查（角色：Review Agent）
- **阶段目标**：逐条核对验收标准 AC-01~AC-11，输出审查报告
- 若全部通过 → ✅ 完成，更新 `docs/features/INDEX.md` 状态
- 若有未通过 → 退回 Dev Agent 并附具体意见

## 3. 验收标准

### 功能验收
- [ ] AC-01 核心功能通过：4 页全部正常显示，湿度计显示 SHT30 本地湿度 + 指针指向正确角度（30~330），双温对比 LOCAL(SHT30) vs REGIONAL(ESP32)
- [ ] AC-02 异常处理正确：SHT30 离线 → 湿度计页显示 "--%" + "No SHT30"；双温对比页 LOCAL 优雅降级（"--.-" / "None"），温差行隐藏，不崩溃；ESP32 无数据 → REGIONAL "--.-"

### 代码质量
- [ ] AC-03 编译无错无警告（Keil ARMCC V5, 0 Error）
- [ ] AC-04 回归检查通过：`bash .claude/scripts/check-firmware.sh` 18 项全绿
- [ ] AC-05 函数<=50行，文件<=800行（`temp_mode.c` 当前 ~247 行，预计新增 ~250 行，合计 <800）

### 安全红线
- [ ] AC-06 无硬编码密钥/Token/密码
- [ ] AC-07 输入已消毒：SHT30 原始读数通过驱动层换算，湿度值已校验范围
- [ ] AC-08 日志已脱敏（不涉及敏感数据）

### 测试与边界
- [ ] AC-09 新增测试用例：6 条（TC01-TC06，见阶段 4）
- [ ] AC-10 回归测试通过：现有 Clock/Timer/Settings 三模式不受影响
- [ ] AC-11 实际修改未超出任务边界：仅 `temp_mode.c/h` + `pointer_engine.c/h`，不改驱动层、不碰 hal_msp、不新增文件

## 4. 执行日志
| 时间 | 阶段 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| 2026-06-21 00:08 | 阶段1 | Dev | I2C2 配置已确认 (PF0/PF1 AF4 OD 100kHz)，SHT30 探测成功 `Device detected at 0x44` | 编译时发现 FreeRTOS 源文件被误删，`git checkout` 恢复后编译通过 |
| 2026-06-21 00:10 | 阶段2 | Dev | `pointer_set_humidity()` 实现，湿度 0-100% → 30°-330° 弧线 | 无 |
| 2026-06-21 00:15 | 阶段3 | Dev | `temp_mode.c` bool→enum 重构，4 页轮转：天气→湿度计→双温对比→设备信息 | 无 |
| 2026-06-21 00:30 | 阶段4 | Test | TC01-TC06 全部通过: 4页显示正确、SHT30离线降级正常、编译0 Error | SHT30 VCC 线松过一次，重新插紧恢复 |
